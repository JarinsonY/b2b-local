interface DtoUpdateStatus {
    active: boolean;
}

interface ICountry {
    code: string;
    currency: string;
    id: number;
    language: string;
    name: string;
}

interface IOption {
    label: string;
    value: string;
}
